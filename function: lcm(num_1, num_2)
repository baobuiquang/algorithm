//Function: lcm (least common multiple)
int lcm(int a, int b)
{
	int max = a * b;
	//Step = max(a,b)
	int step = a * (a > b) + b * (a <= b);
	for (int i = step; i <= max; i += step)
	{
		if ((i % a == 0) && (i % b == 0))
		{
			return i;
		}
	}
	return -1;
}
